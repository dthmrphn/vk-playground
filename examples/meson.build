executable('triangle', 'triangle.cpp', triangle_shaders,
  dependencies: deps,
  link_with: libcommon,
  include_directories : libcommon_inc,
)

executable('simple', 'simple.cpp',
  dependencies: deps,
  link_with: libcommon,
  include_directories : libcommon_inc,
)
